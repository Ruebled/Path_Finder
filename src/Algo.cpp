#include <unistd.h>
#include "Matrix.h"
#include "Grid.h"

void A_star(Matrix& matrix){
	for(int i=0; i<matrix.height(); i++){
		for(int j=0; j<matrix.width(); j++){
			matrix[i][j] = Type(path);
			usleep(50000);
		}
	}
	return;
}
